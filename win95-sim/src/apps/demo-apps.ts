/**
 * Demo Apps - placeholder for any future demo applications.
 *
 * The original test apps (Test App A, Test App B, Shared Context, Explorer)
 * have been removed as they were development/debugging tools. The Workspace
 * Manager and CP/M Terminal provide much better demonstrations of the system.
 */

import type { Desktop } from '../desktop';

/**
 * Register demo apps with the desktop taskbar.
 * Currently empty - demo apps have been removed.
 */
export function registerDemoApps(_desktop: Desktop): void {
  // No demo apps registered - use Workspace or CP/M Terminal instead
}
